以下の3タスクを順番に実行してください。

## 重要なルール

1. **1タスクずつ順番に実行**(並行禁止)
2. **判断に迷ったら、その箇所はTODOコメントを残してスキップ**(勝手に推測しない)
3. **各タスク完了時に必ずコミット**(プッシュはしない)
4. **致命的なエラーが出たら、そこで停止して状態を保全**
5. **CLAUDE.md と docs/strategy.md と docs/refactor-strategy.md の方針を厳守**

---

## タスク1: /solutions/construction のリンク切れ修正(最優先・最初に実行)

### 背景
docs/demo-status-construction.md に記載のとおり、relatedDemos に未実装デモが
2件含まれており、リンク切れが発生している。これを今すぐ修正する。

### 作業内容

**ファイル**: `src/lib/content/industry-showcase.ts` (行46-70付近)

**変更内容**:
relatedDemos を以下に変更:

```typescript
relatedDemos: [
  { slug: "construction-shadow-foreman", title: "現場監督の影武者" },
  { slug: "internal-knowledge-share-bot", title: "社内ナレッジ共有BOT(建設・設備モード)" },
  { slug: "morning-meeting-daily-draft", title: "朝礼メモから日報ドラフト" },
]
```

ただし、上記3つのデモが実際に動作する状態か(URLが有効か)を
事前に確認してから差し替えること。

### 確認事項
- 各デモのURLが /experience/[slug] でアクセス可能か
- Sanity への登録が必要なデモがあれば、その旨をコメントで残す

### コミットメッセージ
```
fix(solutions): 建設業ハブのリンク切れを実装済みデモに差し替え

- photo-inspection-report と daily-weekly-report-summary は未実装のため削除
- 代替として internal-knowledge-share-bot と morning-meeting-daily-draft を追加
```

### 完了したら
タスク2に進む前に git status で状態を報告してください。

---

## タスク2: /about ページの実装

### 前提
docs/refactor-strategy.md の **Phase F-1: /about ページ改善** を参照すること。
設計書に8セクション分のコピーが全文記載されている。

### 作業内容

#### 段階1: site-copy.ts の aboutCopy 更新

ファイル: `src/lib/content/site-copy.ts`

aboutCopy 定数を、設計書のセクション1〜8のコピーに準拠して全面書き換え。
ただし、以下のプレースホルダーは触らないこと(私が後で実値に置き換える):

- 所在地: `[所在地を記載予定]`
- 設立日: `[設立日を記載予定]`
- 代表取締役: `[代表名を記載予定]`

これらは TODO コメントで明示すること。

コミットメッセージ:
```
refactor(about): aboutCopy を設計書(Phase F-1)に基づき全面刷新
```

#### 段階2: AboutPageContent.tsx のレイアウト刷新

ファイル: `src/components/about/AboutPageContent.tsx`

設計書のセクション1〜8の構成に従ってレイアウトを刷新。
必要に応じて新規セクションコンポーネントを追加。

ただし、判断に迷う実装(レイアウトの細かいCSSなど)は、
シンプルな実装で先に動くようにし、後で調整できる構造にすること。

コミットメッセージ:
```
refactor(about): /about ページを8セクション構成に刷新
```

### 完了条件
- [ ] /about にアクセスして、設計書通りの8セクションが表示される
- [ ] 顔写真は不使用
- [ ] 会社概要のプレースホルダーは TODO として残してある
- [ ] CTAから /contact への動線がある
- [ ] エラーなくビルドできる

### 完了したら
タスク3に進む前に git status と動作確認の所感を報告してください。

---

## タスク3: property-exterior-photo-memo の建設業向け転用(余裕があれば)

### 前提
docs/demo-status-construction.md にある通り、
property-exterior-photo-memo はラベル変更だけで建設業向けに転用可能。

### 作業内容

#### 段階1: 既存デモの建設業向け派生バージョンを作成

新規 slug: `construction-photo-inspection-report` (推奨)

実装方法は以下から選択(コードを読んで適切な方を判断):
- A: `PropertyExteriorPhotoExperience.tsx` をコピーして建設業版を作成
- B: 既存コンポーネントにモードプロップを追加して切替式にする

**判断に迷ったら、A(コピーして別コンポーネント)を選択**してください。
コードベースが綺麗だから。

#### 段階2: 建設業向けのラベル・入力例に変更

レポート(docs/demo-status-construction.md)に書かれた変更内容を実装:
- 「共用部」「照明消灯」「外壁ひび」 → 「足場」「亀裂」「保護具」など
- 入力例も建設現場向けに

具体的なラベル例で迷ったら、TODO コメントを残してスキップしてOK。

#### 段階3: タスク1で削除した参照を、新規デモに差し替え

industry-showcase.ts の relatedDemos に新しい slug を追加。

### コミットメッセージ
```
feat(experience): 建設業向け写真点検報告デモを追加

property-exterior-photo-memo を派生して、
construction-photo-inspection-report を新規実装。
solutions/construction の relatedDemos に追加。
```

### 完了したら
git status と動作確認の所感を報告してください。

---

## 全タスク完了後

最後に以下を報告してください:

1. 完了したタスク一覧
2. スキップした箇所(TODO コメント残した部分)
3. 動作確認で気になった点
4. 私が起きてから判断が必要な事項

ミーティングから戻ったら、報告を確認して次のステップを決めます。